-------------------------------------------------------------------------------
-- devicons
-------------------------------------------------------------------------------

local devicons = require 'nvim-web-devicons'

devicons.setup {
  override = {},
  default = true,
}
